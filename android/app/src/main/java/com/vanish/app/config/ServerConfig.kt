package com.vanish.app.config

/**
 * Socket.IO server URL. Use your machine IP for emulator (e.g. "http://10.0.2.2:3000" for Android emulator localhost).
 */
object ServerConfig {
    const val SOCKET_URL = "http://10.0.2.2:3000"
}
